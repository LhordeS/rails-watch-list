<div class="container">
  <h1><%= @list.name %></h1>
  <ul>
    <% @list.bookmarks.each do |bookmark| %>
      <li><%= bookmark.comment %></li>
    <% end %>
  </ul>
  <%= link_to "Add to this list", new_list_bookmark_path(@list) %>
  <%= link_to "Back to Lists", lists_path %>
</div>
